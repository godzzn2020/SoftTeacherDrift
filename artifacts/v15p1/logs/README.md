# v15p1 日志归档与定位

v15p1 的日志在仓库中通常以 `logs_v15p1vote_s0/`、`logs_v15p1vote_s1/` 等形式存在（或已归档到本目录）。

若目录较大不移动，请至少依照 `artifacts/v15p1/MANIFEST.md` 中的表格产物，结合其中的 `run_index_json` 与 `log_path` 定位到 `*.summary.json`。

